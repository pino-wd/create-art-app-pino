<% const runScript = options.packageManager === 'npm' ? 'npm run' : options.packageManager %>
---
title: CLI 命令参考
type: reference
status: active
owner: "@team"
lastReviewed: 2026-01-01
---

# CLI 命令参考

## 开发

| 命令           | 说明                 |
| -------------- | -------------------- |
| `<%= runScript %> dev`     | 启动 Vite 开发服务器 |
| `<%= runScript %> build`   | 生产构建             |
| `<%= runScript %> preview` | 预览构建产物         |

## 代码质量

| 命令              | 说明                |
| ----------------- | ------------------- |
| `<%= runScript %> lint`       | ESLint 检查         |
| `<%= runScript %> lint:fix`   | ESLint 自动修复     |
| `<%= runScript %> type-check` | TypeScript 类型检查 |

## 文档治理

| 命令             | 说明                  |
| ---------------- | --------------------- |
| `<%= runScript %> docs:lint` | 运行全部 4 道文档护栏 |

`docs:lint` 依次执行：

1. **lint-frontmatter** — 校验主文档区 frontmatter 必填字段与枚举值
2. **lint-naming** — 校验文件名 kebab-case、日期前缀、ADR 编号前缀
3. **lint-no-private-path** — 检测私人绝对路径（`/Users/`、`/home/`、`C:\Users\`）
4. **lint-links** — 检测死链（需安装 [lychee](https://github.com/lycheeverse/lychee)，未安装则跳过）

<% if (options.scaffold.commitChecks) { %>## Git Hooks

| Hook         | 执行内容                                                     |
| ------------ | ------------------------------------------------------------ |
| `pre-commit` | `lint-staged`（ESLint + frontmatter + naming）+ 私人路径检测 |
| `commit-msg` | `commitlint`（Conventional Commits）                         |
| `pre-push`   | `lint` + `type-check` + `docs:lint`                          |
<% } else { %>## Git Hooks

当前创建项目时未启用 Git Hook 自动校验。如需启用，请在脚手架创建时开启“提交校验”选项。
<% } %>
